// Import modules
import { initLoginForm } from './components/loginForm.js';
import { initChatBox } from './components/chatBox.js';

document.addEventListener('DOMContentLoaded', () => {
  // Initialize components
  initLoginForm();
  initChatBox();
});
